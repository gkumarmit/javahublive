spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8888/config-server # optional
      
management:
  endpoints:
    web:
      exposure:
        include:
        - info, health, refresh 